
box
{
	new(xaml-path$ = text?):
		app$ = system.windows.application {};
		app$.set(startup-uri: system.uri(xaml-path$));
		//app$.load-component(system.uri(xaml-path$));
		wpf-application {internal: app$},
		
	self$.run():
		self$.internal.run()
}
